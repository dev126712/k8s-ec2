apiVersion: v1
kind: Service
metadata:
  name: devops-k8s-api-service
  labels:
    app: devops-k8s-api
spec:
  selector:
    app: devops-k8s-api
  ports:
    - protocol: TCP
      port: 3000 # Service port
      targetPort: 3000 # Container port
  type: NodePort # change to LoadBalancer if running in cloud